@model List<CourseView>
@{
    ViewData["Title"] = "Course page";
}

<div class=" row">
    <a asp-controller="Course" asp-action="Create" class="btn btn-info">Create</a>

    <table class="table table-bordered mt-2">
        <thead class="thead-dark">
            <tr>
                <th>Course Id</th>
                <th>Course Name</th>
                <th>Start date</th>
                <th>End date</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
<<<<<<< HEAD
                <tr>
                    <td>@item.CourseId</td>
                    <td>@item.CourseName</td>
                    <td>@item.StartDateStr</td>
                    <td>@item.EndDateStr</td>
                    <td>
                        <a asp-controller="Course" asp-action="Edit" asp-route-id="@item.CourseId" class="text-warning"><i class="fas fa-edit"></i></a>
                        <a asp-controller="Course" asp-action="Details" asp-route-id="@item.CourseId" class="text-primary"><i class="fas fa-eye"></i></a>
                        <a href="javascript:void(0)" class="text-danger" onclick="role.delete('@item.CourseId')"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
=======
            <tr>
                <td>@item.CourseId</td>
                <td>@item.CourseName</td>
                <td>@item.StartDateStr</td>
                <td>@item.EndDateStr</td>
                <td><span class="@(item.Status == 1? "btn btn-primary": "btn btn-success")">@item.StatusName</span></td>
                <td></td>
            </tr>
>>>>>>> origin/development
            }
        </tbody>
    </table>
</div>

